spring.data.mongodb.port=0
spring.data.mongodb.host=localhost

# pretty print JSON output, e.g. for "/api/..." in Browser
# (already exists in some Browsers like Firefox)
spring.jackson.serialization.indent-output=true
